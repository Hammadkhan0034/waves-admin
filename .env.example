# Waves Admin Panel — Environment Variables
# Copy to .env.local for local dev, .env.production for VPS build

# Your NestJS backend URL (no trailing slash)
# Local dev:
VITE_API_URL=http://localhost:3001

# Production (your VPS IP or domain):
# VITE_API_URL=https://api.wavesmiami.com
